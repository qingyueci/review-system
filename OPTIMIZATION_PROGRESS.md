# 复盘驾驶舱优化记录

## 约束

- 原 Excel 排版生成器冻结，不修改其样式和工作表结构。
- Word 模板暂不调整，只优化内容选择、任务流程和稳定性。
- API Key、服务令牌、本机数据库、历史输出和用户复盘资料不进入 Git。
- 前端与本机后端分别保留 Git 历史；站点前端继续使用现有独立仓库。

## 当前阶段

### 0. Git 安全基线

- 状态：已完成
- 目标：建立根目录代码仓库，排除密钥、数据库、生成文件和用户资料。
- 结果：后端、启动器和优化记录已进入根仓库；前端继续使用独立仓库。

### 1. 生成控制

- 状态：已完成
- 目标：允许选择“同时生成 / 仅 Excel / 仅 Word”，并可单独重试失败项。
- 结果：后端支持按分支生成和失败分支重试；前端提供三种生成模式。

### 2. 任务持久化

- 状态：已完成
- 目标：本机服务重启后仍能查询最近任务、失败原因和已生成文件。
- 结果：生成任务写入本机 SQLite；重启时会标记中断分支，并保留成功结果。

### 3. 结构拆分

- 状态：已完成
- 目标：逐步拆分过大的后端调度文件与前端页面，保持行为不变。
- 结果：后端已拆成任务管理、SQLite 任务仓库、文件管理、生成服务、文件输入、请求模型和分析解析；前端已拆出本机服务通信，以及今日复盘、布局分析、知识库、历史文档四个组件。

### 4. API 额度保护

- 状态：已完成
- 目标：避免重复点击或重复重试对同一份复盘重复调用模型。
- 结果：相同日期、相同正文和相同生成模式的运行中任务只保留一份，页面会自动接回原任务。

### 5. 任务记录保留

- 状态：已完成
- 目标：防止本机任务数据库无限增长，同时不误删正在运行的任务。
- 结果：默认保留最近 100 条；只清理超出上限且已经成功或失败的旧任务。

### 6. 混合 RAG 检索

- 状态：已完成
- 目标：关键词检索与本地向量检索合并，并加入来源、时间和题材相关度。
- 结果：本人回复、主帖、人工体系、社区评论按 1.00 / 0.90 / 0.82 / 0.55 加权；每条引用保留混合相关度和检索方式。

### 7. 运行记录

- 状态：已完成
- 目标：定位超时、额度骤降和单分支失败。
- 结果：SQLite 任务记录包含 Excel/Word 分支耗时、引用、失败类型、模型名称和供应商返回的 token 用量；前端历史页可直接查看。

### 8. 知识库增量维护

- 状态：已完成
- 目标：避免每日重新扫描高阅读量核心帖，同时保留移出近期窗口的历史资料。
- 结果：核心 20 篇建立后冻结；日常只维护近期 10 篇。旧近期帖改为低权重历史归档，原文、回复和检索片段不删除，也不再重复爬取。

## 验证记录

- 基线后端测试：25 项通过
- 基线前端测试：构建通过，4 项页面测试通过
- 第一批优化后端测试：27 项通过
- 第一批优化前端测试：构建通过，4 项页面测试通过
- 第一批优化站点：第 8 版已发布
- 第二批优化后端测试：29 项通过
- 第二批优化前端测试：构建通过，4 项页面测试通过
- 第二批优化站点：第 9 版已发布
- 第三批优化后端测试：30 项通过
- 第三批优化前端测试：构建、4 项页面测试和代码检查通过
- 第三批优化站点：第 10 版已发布
- 第四批优化后端测试：31 项通过
- 第四批优化前端测试：构建、4 项页面测试和代码检查通过
- 根仓库回退点：`4753355`（安全基线）、`a12dd52`（任务持久化与重试）
- 前端仓库回退点：`88f079f`（生成模式与重试界面）
- 第二批根仓库回退点：`f668380`（后端模块拆分）
- 第二批前端仓库回退点：`c0e2289`（四个页面组件拆分）
- 第三批根仓库回退点：`89e2818`（混合检索与运行诊断）
- 第三批前端仓库回退点：`1affcae`（运行记录界面）
- 真实 Kimi 调用：默认不执行，避免消耗额度；仅在用户主动测试时运行。
